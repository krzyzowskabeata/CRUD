import React from 'react';

const Header = () => (
    <div className="container header">
        <h2>CRUD</h2>
        <div>
            <h4>Log in vs User</h4>
            <h4>Log out</h4>
        </div>
    </div>
);

export default Header;